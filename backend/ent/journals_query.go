// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"selfregi/ent/journals"
	"selfregi/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// JournalsQuery is the builder for querying Journals entities.
type JournalsQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.Journals
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JournalsQuery builder.
func (jq *JournalsQuery) Where(ps ...predicate.Journals) *JournalsQuery {
	jq.predicates = append(jq.predicates, ps...)
	return jq
}

// Limit adds a limit step to the query.
func (jq *JournalsQuery) Limit(limit int) *JournalsQuery {
	jq.limit = &limit
	return jq
}

// Offset adds an offset step to the query.
func (jq *JournalsQuery) Offset(offset int) *JournalsQuery {
	jq.offset = &offset
	return jq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jq *JournalsQuery) Unique(unique bool) *JournalsQuery {
	jq.unique = &unique
	return jq
}

// Order adds an order step to the query.
func (jq *JournalsQuery) Order(o ...OrderFunc) *JournalsQuery {
	jq.order = append(jq.order, o...)
	return jq
}

// First returns the first Journals entity from the query.
// Returns a *NotFoundError when no Journals was found.
func (jq *JournalsQuery) First(ctx context.Context) (*Journals, error) {
	nodes, err := jq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{journals.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jq *JournalsQuery) FirstX(ctx context.Context) *Journals {
	node, err := jq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Journals ID from the query.
// Returns a *NotFoundError when no Journals ID was found.
func (jq *JournalsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{journals.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jq *JournalsQuery) FirstIDX(ctx context.Context) int {
	id, err := jq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Journals entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Journals entity is found.
// Returns a *NotFoundError when no Journals entities are found.
func (jq *JournalsQuery) Only(ctx context.Context) (*Journals, error) {
	nodes, err := jq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{journals.Label}
	default:
		return nil, &NotSingularError{journals.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jq *JournalsQuery) OnlyX(ctx context.Context) *Journals {
	node, err := jq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Journals ID in the query.
// Returns a *NotSingularError when more than one Journals ID is found.
// Returns a *NotFoundError when no entities are found.
func (jq *JournalsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{journals.Label}
	default:
		err = &NotSingularError{journals.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jq *JournalsQuery) OnlyIDX(ctx context.Context) int {
	id, err := jq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JournalsSlice.
func (jq *JournalsQuery) All(ctx context.Context) ([]*Journals, error) {
	if err := jq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return jq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (jq *JournalsQuery) AllX(ctx context.Context) []*Journals {
	nodes, err := jq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Journals IDs.
func (jq *JournalsQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := jq.Select(journals.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jq *JournalsQuery) IDsX(ctx context.Context) []int {
	ids, err := jq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jq *JournalsQuery) Count(ctx context.Context) (int, error) {
	if err := jq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return jq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (jq *JournalsQuery) CountX(ctx context.Context) int {
	count, err := jq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jq *JournalsQuery) Exist(ctx context.Context) (bool, error) {
	if err := jq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return jq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (jq *JournalsQuery) ExistX(ctx context.Context) bool {
	exist, err := jq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JournalsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jq *JournalsQuery) Clone() *JournalsQuery {
	if jq == nil {
		return nil
	}
	return &JournalsQuery{
		config:     jq.config,
		limit:      jq.limit,
		offset:     jq.offset,
		order:      append([]OrderFunc{}, jq.order...),
		predicates: append([]predicate.Journals{}, jq.predicates...),
		// clone intermediate query.
		sql:    jq.sql.Clone(),
		path:   jq.path,
		unique: jq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Journals.Query().
//		GroupBy(journals.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jq *JournalsQuery) GroupBy(field string, fields ...string) *JournalsGroupBy {
	grbuild := &JournalsGroupBy{config: jq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := jq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return jq.sqlQuery(ctx), nil
	}
	grbuild.label = journals.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Journals.Query().
//		Select(journals.FieldCreatedAt).
//		Scan(ctx, &v)
func (jq *JournalsQuery) Select(fields ...string) *JournalsSelect {
	jq.fields = append(jq.fields, fields...)
	selbuild := &JournalsSelect{JournalsQuery: jq}
	selbuild.label = journals.Label
	selbuild.flds, selbuild.scan = &jq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a JournalsSelect configured with the given aggregations.
func (jq *JournalsQuery) Aggregate(fns ...AggregateFunc) *JournalsSelect {
	return jq.Select().Aggregate(fns...)
}

func (jq *JournalsQuery) prepareQuery(ctx context.Context) error {
	for _, f := range jq.fields {
		if !journals.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jq.path != nil {
		prev, err := jq.path(ctx)
		if err != nil {
			return err
		}
		jq.sql = prev
	}
	return nil
}

func (jq *JournalsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Journals, error) {
	var (
		nodes = []*Journals{}
		_spec = jq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Journals).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Journals{config: jq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (jq *JournalsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jq.querySpec()
	_spec.Node.Columns = jq.fields
	if len(jq.fields) > 0 {
		_spec.Unique = jq.unique != nil && *jq.unique
	}
	return sqlgraph.CountNodes(ctx, jq.driver, _spec)
}

func (jq *JournalsQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := jq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (jq *JournalsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   journals.Table,
			Columns: journals.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: journals.FieldID,
			},
		},
		From:   jq.sql,
		Unique: true,
	}
	if unique := jq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := jq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, journals.FieldID)
		for i := range fields {
			if fields[i] != journals.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jq *JournalsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jq.driver.Dialect())
	t1 := builder.Table(journals.Table)
	columns := jq.fields
	if len(columns) == 0 {
		columns = journals.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jq.sql != nil {
		selector = jq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jq.unique != nil && *jq.unique {
		selector.Distinct()
	}
	for _, p := range jq.predicates {
		p(selector)
	}
	for _, p := range jq.order {
		p(selector)
	}
	if offset := jq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JournalsGroupBy is the group-by builder for Journals entities.
type JournalsGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jgb *JournalsGroupBy) Aggregate(fns ...AggregateFunc) *JournalsGroupBy {
	jgb.fns = append(jgb.fns, fns...)
	return jgb
}

// Scan applies the group-by query and scans the result into the given value.
func (jgb *JournalsGroupBy) Scan(ctx context.Context, v any) error {
	query, err := jgb.path(ctx)
	if err != nil {
		return err
	}
	jgb.sql = query
	return jgb.sqlScan(ctx, v)
}

func (jgb *JournalsGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range jgb.fields {
		if !journals.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := jgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (jgb *JournalsGroupBy) sqlQuery() *sql.Selector {
	selector := jgb.sql.Select()
	aggregation := make([]string, 0, len(jgb.fns))
	for _, fn := range jgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(jgb.fields)+len(jgb.fns))
		for _, f := range jgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(jgb.fields...)...)
}

// JournalsSelect is the builder for selecting fields of Journals entities.
type JournalsSelect struct {
	*JournalsQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (js *JournalsSelect) Aggregate(fns ...AggregateFunc) *JournalsSelect {
	js.fns = append(js.fns, fns...)
	return js
}

// Scan applies the selector query and scans the result into the given value.
func (js *JournalsSelect) Scan(ctx context.Context, v any) error {
	if err := js.prepareQuery(ctx); err != nil {
		return err
	}
	js.sql = js.JournalsQuery.sqlQuery(ctx)
	return js.sqlScan(ctx, v)
}

func (js *JournalsSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(js.fns))
	for _, fn := range js.fns {
		aggregation = append(aggregation, fn(js.sql))
	}
	switch n := len(*js.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		js.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		js.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := js.sql.Query()
	if err := js.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
